/// <reference path="../unit-test/repository/user-repository-mock.ts" />

import {AuthController} from './authcontroller';
import {UserRepositoryMock} from '../unit-test/repository/user-repository-mock';

describe('AuthControllerFunc', () => {
    var ut;
    //it('Calls function createAuthStrategy', () => {
    //    var authController = {
    //        addRoutes: function () {
    //        }
    //    };

    //    spyOn(authController, "addRoutes");
    //    authController = new AuthController("/", <any>UserRepositoryMock);
    //   // authController.createAuthStrategy();
    //    expect(authController.addRoutes).toHaveBeenCalled();
    //})
});
