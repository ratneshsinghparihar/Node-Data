//import * as Express from "express";


export default class BaseRepository {// extends d1.repo {
    constructor(path1, fn) {

        //super(path1, fn);

        //var d = new d1.repo(path1, fn);
    }
}
//module.exports = BaseRepository;